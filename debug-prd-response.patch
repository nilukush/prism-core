--- a/frontend/src/app/app/prds/new/page.tsx
+++ b/frontend/src/app/app/prds/new/page.tsx
@@ -232,8 +232,11 @@ export default function NewPRDPage() {
       })
 
       // The backend returns the response directly, not wrapped in data
+      console.log('PRD Generation Response:', response)
       if (response.success) {
+        console.log('PRD Content:', response.prd)
         setGeneratedPRD(response.prd)
+        console.log('State after setting PRD:', response.prd)
         setPrdMetadata({
           productName: formData.productName,
           targetAudience: formData.targetAudience,